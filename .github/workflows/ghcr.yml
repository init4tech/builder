name: Release GHCR

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  ghcr:
    uses: init4tech/actions/.github/workflows/ghcr.yml@main
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
